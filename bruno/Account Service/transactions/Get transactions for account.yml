info:
  name: Get transactions for account
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseurl}}/api/v1/transactions/reference/TXN-DC06DF522DE94C589A4CACE1"
  headers:
    - name: x-api-key
      value: "{{api_key}}"
  auth: inherit

runtime:
  variables:
    - name: baseurl
      value: http://localhost:8080
    - name: api_key
      value: MGX_204406

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
