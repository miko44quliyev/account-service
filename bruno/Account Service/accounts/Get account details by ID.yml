info:
  name: Get account details by ID
  type: http
  seq: 5

http:
  method: GET
  url: "{{baseUrl}}/api/v1/accounts/e264b262-b689-444c-b6dd-73502e382381"
  headers:
    - name: x-api-key
      value: "{{api_key}}"
  auth: inherit

runtime:
  variables:
    - name: baseUrl
      value: http://localhost:8080
    - name: api
      value: MGX_204406
    - name: api_key
      value: MGX_204406

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
