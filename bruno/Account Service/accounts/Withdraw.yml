info:
  name: Withdraw
  type: http
  seq: 7

http:
  method: POST
  url: "{{baseUrl}}/api/v1/accounts/e264b262-b689-444c-b6dd-73502e382381/withdraw"
  headers:
    - name: x-api-key
      value: "{{api_key}}"
  body:
    type: json
    data: |-
      {
        "amount": 200.00,
        "description": "ATM withdrawal"
      }
  auth: inherit

runtime:
  variables:
    - name: baseUrl
      value: http://localhost:8080
    - name: api
      value: MGX_204406
    - name: api_key
      value: MGX_204406

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
